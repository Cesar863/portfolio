name: Build App

on: 
  push: 
    branches: 
      - main

jobs: 
  build-app:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install dependencies
        run: npm install

      - name: build app
        run: npm run build
